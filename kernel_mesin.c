/* --- Ini adalah kode Bahasa C --- */
#include <stdio.h>

int main() {
    // 1. Variabel dengan tipe data yang ketat
    int skor = 100;
    char grade = 'A';

    // 2. Menampilkan output ke layar
    printf("Selamat, Master!\n");
    printf("Skor anda adalah: %d dengan Grade: %c\n", skor, grade);

    return 0;
}
